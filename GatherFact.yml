---
- name: Simple Windows Playbook
  hosts: 192.168.62.79
  gather_facts: no

  tasks:
    - name: Create a folder
      win_file:
        path: C:\DemoFolder
        state: directory

    - name: Ensure Windows Update service is running
      win_service:
        name: wuauserv
        start_mode: auto
        state: started
